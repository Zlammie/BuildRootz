{
  "name": "buildrootz",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm start --prefix backend",
    "start": "concurrently -k \"npm run frontend\" \"npm run backend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
